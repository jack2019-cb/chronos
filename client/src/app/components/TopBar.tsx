import React from "react";

const TopBar: React.FC = () => {
  return (
    <header className="calendar-plus-topbar">
      <div>TopBar (App Name | View Switcher | Export | Share)</div>
    </header>
  );
};

export default TopBar;
