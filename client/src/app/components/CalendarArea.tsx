import React from "react";

const CalendarArea: React.FC = () => {
  return (
    <main className="calendar-plus-area">
      <div>Main Calendar Area (Grid, Drag/Drop, Live Preview)</div>
    </main>
  );
};

export default CalendarArea;
